#pragma once

#include <stdfloat>

/**
 * A packet containing data from the encoders.
 * TODO: This is a placeholder structure. Update when the encoder data is known.
 */
struct EncoderData {
    /** The encoder value. */
    std::float64_t value;
};
